async function main() {
    // compile them in our code 
    // compile them separately
    //HTTP://127.0.0.1:7545
    
}

main().then(() => process.exit(0)).catch((error) => {
    console.error(error);
    process.exit(1);
});